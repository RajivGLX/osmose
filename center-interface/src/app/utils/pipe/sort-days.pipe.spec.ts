import { SortDaysPipe } from './sort-days.pipe';

describe('SafePipe', () => {
    it('create an instance', () => {
        const pipe = new SortDaysPipe();
        expect(pipe).toBeTruthy();
    });
});