import { SortSlotsPipe } from './sort-slots.pipe';

describe('SafePipe', () => {
    it('create an instance', () => {
        const pipe = new SortSlotsPipe();
        expect(pipe).toBeTruthy();
    });
});